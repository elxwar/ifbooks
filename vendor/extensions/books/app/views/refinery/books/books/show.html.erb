<div class="container schools">
  <section id ="left_margin">
    <%= image_fu @book.margin_image, nil %>
  </section>
  
  <div class ="left_of_page">
    <section id="title_bar_content">
      <span><%=raw @book.title %></span>
    </section>

    <section id='book_body_content'>
      <%=raw @book.description_for_schools %>
    </section>

    TEST BOOK FILE LINKS - will be restricted to only logged on users but visible to all for testing!
    <br/>
    If there are no links below, none have been added to the book yet...
    <section>

      <p>
        <% if @book.file1.present? %>
          <h1>File1</h1>
          <%= link_to 'file1', @book.file1.url %>
          <br/>
        <% end %>

        <% if @book.file2.present? %>
          <h1>File2</h1>
          <%= link_to 'file2', @book.file2.url %>
          <br/>
        <% end %>

        <% if @book.file3.present? %>
          <h1>File3</h1>
          <%= link_to 'file3', @book.file3.url %>

        <% end %>

    </section>




  </div>
  <div class="right_of_page">
    <div class =" vid_frame">
      <%= raw @book.video_url %>
    </div>

    <div id="book_register">
      To register for <em><%=raw @book.title %></em><br /><br />
      <div class="book_buttons">
        <%#= link_to "CLICK HERE", main_app.register_path(:id => @book.id), :class=>"button" %>
        <%= link_to "CLICK HERE", main_app.register_path(:id => @book.id), :class=>"button" %>

      </div>
    </div>
  </div>
  
  <%#******************COMMENTED OUT*****************%>
  <% if false %>
     <%# content_for :body_content_right do %>
      <%#*<aside>%>
        <%#*<h2><%= t('.other') %></h2>%>
        <%#*<ul id="books">%>
          <%# @books.each do |book| %>
            <%#*<li>%>
              <%#= link_to book.title, refinery.books_book_path(book) %>
            <%#*</li>%>
          <%# end %>
        <%#*</ul>%>
      <%#*</aside>%>
    <%# end %>

    <%#= render '/refinery/content_page' %>
  <% end %>
  <%#*****************END OF COMMENTED OUT*****************%>

</div>