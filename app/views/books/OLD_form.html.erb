<%= form_for(@book) do |f| %>
  <% if @book.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@book.errors.count, "error") %> prohibited this ifgroup from being saved:</h2>

      <ul>
      <% @book.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <p id="notice"><%= notice %></p>
  Hello!!!
  <div>
    <%= f.label :title %><br />
    <%= f.text_field :title, size: 30 %>
  </div>
  <div>
    <%= f.label :author %><br />
    <%= f.text_field :author, size: 30 %>
  </div>

<% end %>
